# CenterDevice API credentials
# Base URL for CenterDevice API
CD_BASE_URL=https://api.centerdevice.de/v2
# Auth URL for token refresh
CD_AUTH_URL=https://auth.centerdevice.de
# OAuth client credentials (same as mcp-stack centerdevice)
CD_CLIENT_ID=
CD_CLIENT_SECRET=
# Refresh token (obtain via CenterDevice OAuth flow)
CD_REFRESH_TOKEN=
